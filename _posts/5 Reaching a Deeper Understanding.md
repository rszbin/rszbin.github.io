Fixed Value - variable whose value does not changed
Stepper - iterator for a loop, something that counts
Flag - indicate something has happened or is the case
Walker - traverses a data structure, similar to a Stepper
    - stepper known beforehand
    - walker variable that traverses a data structure in a way unknown ebfore the loop starts, like a pointer. linked list variable, search index in a binary traverses
most recent holder - most recent value encountered when going through a list of vlaues.
most wanted holder - value you are searching for (or best value found so far)
gatherer - collects data and aggregates it into one value
    sum, where sum+= list[i]
container - multiple elements that can be (list, array)
follower - previous or subsequent value - pointer that points to a previous element
Organizer - variable has to be transformed in some way for further processing, used for re-arranging to storing vlaues differently

Temporary - varibles used only briefly, temp, t etc.

5.2
1. find a focal point
2. determine the slice of code related to the focal point
3. write down what you learned about the code

5.4 reading text is similar to reading code
because we lack practice, good strategies and good schemata, we often must
rely on the much slower praxis of reading code line by line or stepping through it
with a debugger.

this in turn leads to situations where coders prefer to write their own code

5.4.1 
Even if variable names ar obfuscated, participants were reading other elements o fthe code (ie keywords) and attmepting to draw meaning from them
this is the same thing we do when reading words in natural language text

Language ability determined how quickly students passed through the Code Academy course

Students who read well learn a lot in general, while students who struggle with reading do not learn as quickly or easily.

For programming accuracy on the example task, general cognitive skills (memory and reasoning) mattered most.

Learning to follow the call stack is a practice that comes with experience.